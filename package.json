{
  "name": "wg-easy-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "WireGuard (wg-easy) + tun2socks — premávka WG klientov cez SOCKS5 proxy",
  "scripts": {
    "docker:build": "VERSION=$(node -p \"require('./package.json').version\") && docker build --platform linux/amd64 -t reg.kubov.link/ezworkz/wg-easy-proxy:${VERSION} .",
    "docker:push": "VERSION=$(node -p \"require('./package.json').version\") && docker push reg.kubov.link/ezworkz/wg-easy-proxy:${VERSION}",
    "docker:release": "VERSION=$(node -p \"require('./package.json').version\") && pnpm docker:build && pnpm docker:push && docker tag reg.kubov.link/ezworkz/wg-easy-proxy:${VERSION} reg.kubov.link/ezworkz/wg-easy-proxy:latest && docker push reg.kubov.link/ezworkz/wg-easy-proxy:latest",
    "version:show": "node -p \"require('./package.json').version\"",
    "proxies:check": "node check-proxies.mjs"
  }
}
